# delphi-FreeTalk
WinPopup analog for LAN messaging

Обмен сообщениями в локальной сети, аналогично службе сообщений Windows (он же WinPopup). 
Саму службу сообщений не использует - работает с NetBIOS напрямую.

